M3 - Sistemas Distribuidos - Unisinos

Instruções de compilação e execução:

```shell
// Compilar os arquivos
$ javac *.java

// Iniciar o servidor RMI:
$ java ContaBancariaServer

// Iniciar o cliente RMI:
$ java ContaBancariaClient
```